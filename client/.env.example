# Client Environment Variables
# If running through Vite proxy (default in local dev), leave empty or set to /api
# If calling a separate backend URL directly: http://localhost:5000/api
VITE_API_URL=/api
