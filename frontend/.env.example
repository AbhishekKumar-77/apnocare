# For production deployment on Vercel:
# Set this to your deployed Django backend API URL (e.g., https://apnocare-backend.onrender.com/api)
# Leave empty or omitted when running locally with Vite dev proxy (proxies /api to localhost:8000)
VITE_API_URL=
